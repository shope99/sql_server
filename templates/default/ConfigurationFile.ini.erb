; SQLSERVER2008 Configuration File
; Generated by Opscode Chef
[SQLSERVER2008]

; Auto accept the license terms

IACCEPTSQLSERVERLICENSETERMS="<%= node['sql_server']['accept_eula'] ? "True" : "False" %>"

<% unless (node['sql_server']['product_key'].nil? || node['sql_server']['product_key'].empty?)  -%>
; Product Key

PID="<%= node['sql_server']['product_key'] %>"
<% end -%>
; The default is Windows Authentication. Use "SQL" for Mixed Mode Authentication.

SECURITYMODE="<%= node['sql_server']['security_mode'] %>"
SAPWD="<%= node['sql_server']['server_sa_password'] %>"

; The Database Engine root data directory. 

INSTALLSQLDATADIR="<%= node['sql_server']['install_sql_data_dir'] %>"

; Default directory for the Database Engine backup files. 

SQLBACKUPDIR="<%= node['sql_server']['sql_backup_dir'] %>"

; Default directory for the Database Engine user databases. 

SQLUSERDBDIR="<%= node['sql_server']['sql_user_db_dir'] %>"

; Default directory for the Database Engine user database logs. 

SQLUSERDBLOGDIR="<%= node['sql_server']['sql_user_db_log_dir'] %>"

; Directory for Database Engine TempDB files. 

SQLTEMPDBDIR="<%= node['sql_server']['sql_temp_db_dir'] %>"

; Directory for the Database Engine TempDB log files. 

SQLTEMPDBLOGDIR="<%= node['sql_server']['sql_temp_db_log_dir'] %>"

; Specify the Instance ID for the SQL Server features you have specified. SQL Server directory structure, registry structure, and service names will reflect the instance ID of the SQL Server instance.

INSTANCEID="<%= node['sql_server']['instance_name'] %>"

; Specifies a Setup work flow, like INSTALL, UNINSTALL, or UPGRADE. This is a required parameter.

ACTION="Install"

; Specifies features to install, uninstall, or upgrade. The list of top-level features include SQL, AS, RS, IS, and Tools. The SQL feature will install the database engine, replication, and full-text. The Tools feature will install Management Tools, Books online, Business Intelligence Development Studio, and other shared components.

FEATURES="<%= node['sql_server']['features'] %>"

; Displays the command line parameters usage

HELP="False"

; Specifies that the detailed Setup log should be piped to the console.

INDICATEPROGRESS="False"

; Setup will not display any user interface.

QUIET="False"

; Setup will display progress only without any user interaction.

QUIETSIMPLE="False"

; Specifies that Setup should install into WOW64. This command line argument is not supported on an IA64 or a 32-bit system.

X86="False"

; Detailed help for command line argument ROLE has not been defined yet.

ROLE="AllFeatures_WithDefaults"

; Detailed help for command line argument ENU has not been defined yet.

ENU="True"

; Parameter that controls the user interface behavior. Valid values are Normal for the full UI, and AutoAdvance for a simplied UI.

UIMODE="Normal"

; Specify if errors can be reported to Microsoft to improve future SQL Server releases. Specify 1 or True to enable and 0 or False to disable this feature.

ERRORREPORTING="False"

; Specify the root installation directory for native shared components.

INSTALLSHAREDDIR="<%= node['sql_server']['install_dir'] %>"

; Specify the root installation directory for the WOW64 shared components.

INSTALLSHAREDWOWDIR="<%= node['sql_server']['install_dir'] %>"

; Specify the installation directory.

INSTANCEDIR="<%= node['sql_server']['instance_dir'] %>"

; Specify that SQL Server feature usage data can be collected and sent to Microsoft. Specify 1 or True to enable and 0 or False to disable this feature.

SQMREPORTING="False"

; Specify a default or named instance. MSSQLSERVER is the default instance for non-Express editions and SQLExpress for Express editions. This parameter is required when installing the SQL Server Database Engine (SQL), Analysis Services (AS), or Reporting Services (RS).

INSTANCENAME="<%= node['sql_server']['instance_name'] %>"

; Agent account name

AGTSVCACCOUNT="<%= node['sql_server']['agt_svc_account'] %>"

; Auto-start service after installation.

AGTSVCSTARTUPTYPE="<%= node['sql_server']['agt_svc_startup_type'] %>"

; Startup type for Integration Services.

ISSVCSTARTUPTYPE="<%= node['sql_server']['is_svc_startup_type'] %>"

; Account for Integration Services: Domain\User or system account.

ISSVCACCOUNT="<%= node['sql_server']['is_svc_account'] %>"

; The name of the account that the Analysis Services service runs under. 

ASSVCACCOUNT="<%= node['sql_server']['as_svc_account'] %>"

; Controls the service startup type setting after the service has been created.

ASSVCSTARTUPTYPE="<%= node['sql_server']['as_svc_startup_type'] %>"

; The collation to be used by Analysis Services.

ASCOLLATION="<%= node['sql_server']['as_collation']%>"

; The location for the Analysis Services data files.

ASDATADIR="<%= node['sql_server']['as_data_dir'] %>"
; The location for the Analysis Services log files.

ASLOGDIR="<%= node['sql_server']['as_log_dir'] %>"

; The location for the Analysis Services backup files.

ASBACKUPDIR="<%= node['sql_server']['as_backup_dir']%>"

; The location for the Analysis Services temporary files.

ASTEMPDIR="<%= node['sql_server']['as_temp_dir']%>"

; The location for the Analysis Services configuration files.

ASCONFIGDIR="<%= node['sql_server']['as_config_dir']%>"

; Specifies whether or not the MSOLAP provider is allowed to run in process.

ASPROVIDERMSOLAP="1"

; Specifies the list of administrator accounts that need to be provisioned. 

ASSYSADMINACCOUNTS="<%= node['sql_server']['as_sys_admin_accounts']%>"

; A port number used to connect to the SharePoint Central Administration web application.

FARMADMINPORT="0"

; Startup type for the SQL Server service.

SQLSVCSTARTUPTYPE="<%= node['sql_server']['sql_svc_startup_type'] %>"

; Level to enable FILESTREAM feature at (0, 1, 2 or 3).

FILESTREAMLEVEL="0"

; Set to "1" to enable RANU for SQL Server Express.

ENABLERANU="True"

; Specifies a Windows collation or an SQL collation to use for the Database Engine.

SQLCOLLATION="<%= node['sql_server']['sql_collation']%>" 

; Account for SQL Server service: Domain\User or system account.

SQLSVCACCOUNT="<%= node['sql_server']['sql_svc_account']%>""

; Windows account(s) to provision as SQL Server system administrators.

SQLSYSADMINACCOUNTS="<%= node['sql_server']['sql_sys_admin_accounts']%>"

; Provision current user as a Database Engine system administrator for SQL Server 2008 R2 Express.

ADDCURRENTUSERASSQLADMIN="<%= node['sql_server']['addcurrentuserassqladmin'] %>"

; Specify 0 to disable or 1 to enable the TCP/IP protocol.

TCPENABLED="<%= node['sql_server']['tcp_enabled'] %>"

; Specify 0 to disable or 1 to enable the Named Pipes protocol.

NPENABLED="<%= node['sql_server']['np_enabled'] %>"

; Startup type for Browser Service.

BROWSERSVCSTARTUPTYPE="Disabled"

; Specifies which account the report server NT service should execute under.  When omitted or when the value is empty string, the default built-in account for the current operating system.
; The username part of RSSVCACCOUNT is a maximum of 20 characters long and
; The domain part of RSSVCACCOUNT is a maximum of 254 characters long.

RSSVCACCOUNT="<%= node['sql_server']['rs_svc_account'] %>"

; Specifies how the startup mode of the report server NT service.  When
; Manual - Service startup is manual mode (default).
; Automatic - Service startup is automatic mode.
; Disabled - Service is disabled

RSSVCSTARTUPTYPE="<%= node['sql_server']['rs_svc_startup_type'] %>"

; Specifies which mode report server is installed in.
; Default value: "FilesOnly"

RSINSTALLMODE="<%= node['sql_server']['rsinstallmode'] %>"

; Add description of input argument FTSVCACCOUNT 

FTSVCACCOUNT="<%= node['sql_server']['ft_svc_account'] %>"
